export interface IEntry {
  article: string;
  imgSrc: string;
  imgAlt: string;
  title: string;
  brief: string;
  date: string; // YYYY/MM/DD
}

export interface IArticle {
  article: string;
  htmlName: string;
}
